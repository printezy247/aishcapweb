// Update by hand from the Telegram group header. Only figures you can point
// at in the app. Never estimate, never round up. lastCounted renders on the
// public page as a visible date-stamp, the same way the CT-1 card does.
export const COMMUNITY = {
  groupName: "Borak Flip Capital",
  members: 75,
  lastCounted: "2026-09-04",
} as const;
